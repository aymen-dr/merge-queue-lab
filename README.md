# Merge Queue Lab

This private repository contains only harmless fixtures for controlled merge
automation integration testing. No production code, credentials, or user data
belong here.

The `human-merge-approved` label is intentionally required by the merge rule.
The label must not be created or applied during observation-only experiments.
Controlled merge automation integration lab
