name: security-gate-workflow

on:
  pull_request:

permissions:
  contents: read

jobs:
  security-gate:
    name: security-gate
    runs-on: ubuntu-latest
    steps:
      - name: Controlled failing trusted check
        run: |
          echo "Controlled 300-second observation window for MRG-SCOPES-STALE-HEAD-001"
          sleep 300
          echo "Expected fail-closed result after observation window"
          exit 1
